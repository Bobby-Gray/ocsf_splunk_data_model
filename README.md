# ocsf_splunk_data_model
This repo contains some python scripts I wrote to create two variations of valid Splunk Data Model json files mapped to the Open Cybersecurity Schema Framework (OCSF) schema.
